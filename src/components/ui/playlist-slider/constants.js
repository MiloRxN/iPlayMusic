export const CAROUSEL_CONFIG = {
  SWIPE_THRESHOLD: 50, // Minimum swipe distance to trigger a swipe action
  OFFSETS: [-1, 0, 1], // Offsets for previous, center, and next items
};

export const CAROUSEL_STYLES = {
  container: "relative py-8 pt-30 overflow-hidden",
  slider: "flex items-center justify-center gap-8 px-4",
  emptyState: "p-5 text-center",
};
